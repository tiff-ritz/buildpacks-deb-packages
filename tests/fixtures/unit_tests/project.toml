[_]
schema-version = "0.2"

[com.heroku.buildpacks.deb-packages]
install = [
    { name = "git", env = { "GIT_EXEC_PATH" = "{build_dir}/usr/lib/git-core", , "GIT_TEMPLATE_DIR" = "{build_dir}/usr/share/git-core/templates" } },
    { name = "babeld" },
    { name = "ghostscript", skip_dependencies = true, force = true, env = { "GS_LIB" = "{build_dir}/var/lib/ghostscript" } },
]